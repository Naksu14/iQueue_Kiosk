# Example environment variables for the kiosk React app
# Set this to the printer server host:port reachable by the kiosk browser.
# If omitted, the app defaults to http://localhost:4000
REACT_APP_PRINTER_SERVER=http://localhost:4000
