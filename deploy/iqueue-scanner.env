# Sample environment file for iqueue-scanner.service
# Copy to /etc/default/iqueue-scanner and edit values as needed.

# Port the scanner server listens on
PORT=4001

# Path to the evdev symlink under /dev/input/by-id (recommended)
# Find yours with: ls -l /dev/input/by-id
DEVICE_PATH=/dev/input/by-id/usb-SM_SM-2D_PRODUCT_HID_KBW_APP-000000000-event-kbd

# Node binary and script path (adjust if Node is installed elsewhere)
NODE_BIN=/usr/bin/node
SCRIPT_PATH=/home/iqueue/iQueue_Kiosk/scanner-server.js

# Optionally change User= or Group= by creating a systemd drop-in instead of editing the main unit.
